# 개발일지 2026-09-17

## 가입 설문이 프로덕션에서는 아직도 버려지고 있었다

"회원가입 설문이 DB 에 저장되는지 확인"으로 시작한 점검에서 **스키마도 서버도 FE 코드도 멀쩡한데
프로덕션만 옛 번들**이라는 것을 찾았다. 2026-09-09 에 고친 문제가 `app.lighthouse.career` 에서는
그대로 살아 있었다 — 그날 이후 지금까지 프로덕션으로 가입한 사람의 Q1~Q3 는 전부 버려졌다.

```
스키마 User.onboarding        ✅ 2026-09-09
API register/complete-profile ✅ 라이브
FE main/dev + test 셸          ✅ 스테이징 반영
FE 프로덕션(app)               ❌ v0.1.8 = 42d433c 이전
```

### ⚠️ 코드를 읽어서는 절대 못 찾는다 — 배포된 번들을 받아서 봐야 한다

`git log` 도 소스도 "고쳐져 있다"고 말한다. 갈라진 것은 **태그**다.
프로덕션 청크를 직접 받아 문자열을 세는 것으로만 확정된다.

```
curl app.lighthouse.career/ | grep -o 'assets/index-[A-Za-z0-9_-]*\.js'
→ index-CCgTL8Du.js 에서 SignupWizardPage-*.js 이름을 뽑아 받고
grep -c selfAwareness   # prod 0건 / test 1건
```

프로덕션 청크의 실제 내용:

```js
auth/register",{email:l.email,password:l.password,name:l.name,age:l.age,gender:l.gender});
… localStorage.setItem("lh_onboarding_v1", …)   // Q1~Q3 는 여기서 끝
```

> **릴리스 전 가입자의 답은 복구할 수 없다.** 각자 브라우저의 localStorage 에만 있고
> 그 키를 서버로 보내는 경로가 어디에도 없다. 소급 복구는 계획에 넣지 않았다.

### 이메일 가입 vs 카카오 가입 — 무엇이 어디에 저장되나

둘 다 `user_data.users` 의 같은 `User` 문서다. 갈리는 것은 **누가 채우느냐와 계정이 언제 생기느냐**다.

| User 필드 | 이메일 (`register` 1회) | 카카오 (콜백 + `complete-profile`) |
|---|---|---|
| `email` | 입력값 | 동의 시에만. 미동의면 **없음**(sparse unique) |
| `passwordHash` | bcrypt(10) | **없음** → 이메일 로그인 영구 불가 |
| `authProviders` | `[{local, providerId: 이메일}]` | `[{kakao, providerId: 회원번호}]` |
| `name` | 위저드 입력 | 콜백이 닉네임으로 선채움 → 위저드에서 수정 |
| `age`·`gender` | 위저드 입력 | 콜백엔 없음 → `complete-profile` 에서만 |
| `onboarding` | `register` 본문에 동봉 | `complete-profile` 에서만 |
| 계정 생성 시점 | 위저드 **끝** | 위저드 **전**(콜백) |

마지막 줄이 구조적 차이의 뿌리다. 카카오는 계정이 먼저 생기므로 위저드 도중 이탈하면
**나이·성별·Q1~Q3 가 빈 계정이 DB 에 남는다.**

## 카카오만 lastLoginAt 이 안 찍히고 있었다

위 비교표를 만들다 발견했다. `login` 은 `lastLoginAt` 을 갱신하는데 `kakaoCallback` 은
기존 유저를 그대로 return 해서 **카카오 유저만 마지막 접속 집계에서 통째로 빠진다.**

### ⚠️ 여기서 `save()` 를 쓰면 로그인이 죽을 수 있다

`login` 이 `user.save()` 를 쓰니 따라 쓰고 싶어지지만, `save()` 는 **문서 전체 검증을 다시 돌린다.**
옛 계정에 스키마와 어긋난 값이 하나라도 있으면 그 사용자는 **로그인 자체가 실패한다.**
필드 하나 찍자고 낼 대가가 아니다 → `updateOne({$set:{lastLoginAt}})`.

```js
const now = new Date();
await User.updateOne({ _id: existing._id }, { $set: { lastLoginAt: now } });
existing.lastLoginAt = now;   // 호출부가 방금 쓴 값을 보게
```

신규 가입 경로(`User.create`)에도 `lastLoginAt` 을 넣어 두 경로를 맞췄다.

## 나이·성별 검증이 두 벌로 갈라져 있었다

`completeProfile` 에는 1~120 검증이 있는데 `register` 에는 **없었다.** 그래서 같은 잘못된 값이
경로에 따라 400 이 되기도 하고 Mongoose ValidationError → **500** 이 되기도 했다.
`buildOnboarding` 을 공유한 것과 같은 방식으로 `validateProfile` 하나를 둘이 쓰게 했다.

> 9/11 에 "검증을 두 벌로 두면 반드시 어긋난다"고 적어놓고 **나이·성별은 두 벌로 두고 있었다.**
> 원칙을 적은 것과 지킨 것은 다르다.

### ⚠️ 빈 문자열을 통과시키자 다른 곳이 깨졌다

`age: ''` 를 '보내지 않음'으로 보고 통과시키면 `completeProfile` 의 대입부에서
`Number('') === 0` 이 되어 스키마 `min:1` 에 걸린다. 대입부에도 `age !== ''` 를 넣었다.
**검증을 느슨하게 한 대가가 다른 줄에서 나온다.**

## 배포

| 레포 | 커밋 | 상태 |
|---|---|---|
| lighthouse-api | `e6c529d` | run 35175268682 success → **라이브 확인** |
| lighthouse-test | `4286c1d` → **v0.1.9** | run 35180187528 success → **라이브 확인** |

라이브 검증: Actions success 시점 uptime 508035(옛 컨테이너) → **약 2분 뒤 90 으로 떨어져** 교체 확인.
`POST /api/auth/register {age:999}` → **400** `나이는 1~120 사이 정수만 허용됩니다` (수정 전이면 500).
사전 검증: `validateProfile` 16 + `buildOnboarding` 6 = 22건, FE type-check·vitest 50건·prod 빌드 통과.

v0.1.9 로 8커밋(카카오 로그인 FE·소셜 위저드·가입 답변 전송·검사결과 수정)이 함께 나갔다.
프로덕션 번들 실측 — 번들 해시가 `index-CCgTL8Du.js` → `index-Di31OyF-.js` 로 바뀌었고,

```
SignupWizardPage-DYAS3i7C.js
  onboarding:{status:s.q1,concerns:s.q2,selfAwareness:s.q3}
  → complete-profile / selfAwareness / concerns 각 1건 (배포 전 전부 0건)
```

`apps/app` 전용 소셜 가드도 프로덕션 메인 청크에만 있고(`answeredAt`·`social:"1"`)
test 셸에는 0건이다 — 셸이 갈린다는 설명 그대로다.
`GET /api/auth/kakao?redirect=https://app.lighthouse.career/...` → 302, state 의 복귀지가
**app 도메인**, client_secret 미노출.

## 세어보니 유실된 답은 0건이었다

"프로덕션에서 계속 버려지는 중"이라고 판단하고 급히 릴리스했는데, **실제 데이터를 세어보니
피해 건수는 0 이었다.**

```
total 29 / withOnboarding 0 / local 28 / kakao 1
최근 가입 = 2026-09-10 (카카오, 9/10 수동 검증 때 만든 계정)
```

9/9 수정 이후 프로덕션 신규 가입이 **사실상 없었다.** 유일한 9/10 카카오 계정은
`name·age·gender·onboarding` 이 전부 비어 있는데, 이게 바로 9/11 에 고친
"소셜이 위저드를 통째로 건너뛰던" 그 버그의 산물이다.

> ⚠️ **심각도를 데이터 없이 말하지 말 것.** 코드·번들 실측은 "저장 경로가 끊겼다"까지만 말해준다.
> "그래서 몇 명이 손해봤나"는 **세어봐야** 나온다. 릴리스 판단 자체는 옳았지만
> 긴급도 서술은 틀렸다 — 세어보기 전까지는 알 수 없었다.

또 하나: `withOnboarding` 이 0 이므로 **지금 집계 화면을 만들면 전부 빈 값이다.**
릴리스 이후 가입부터 쌓인다.

## 남은 것

- 실계정 카카오 왕복 검증(사람) — 9/10 계정은 `onboarding` 이 없어 **새 가드가 위저드로 되돌릴 대상**이다.
  재로그인하면 P3(`lastLoginAt`)과 가드를 한 번에 확인할 수 있다
- `onboarding` 을 **읽는 곳이 아직 없다**(Admin 언급 0건). 분포 집계 시 `{$exists:true}` 필수 —
  9/9 이전 가입자 28명에겐 필드가 없어 분모가 틀어진다
- 서비스워커 캐시 때문에 브라우저 화면은 옛 번들일 수 있다(9/11 에 실제로 오인) — 판단은 curl 기준

## 가입 설문을 처음으로 읽는 화면

`User.onboarding` 은 9/9 부터 쌓기만 하고 **읽는 코드가 하나도 없었다.** 그 자리를 채웠다.

| 레포 | 커밋 | 내용 |
|---|---|---|
| lighthouse-api | `924b682` | `GET /api/admin/onboarding/stats` (adminAuth 뒤) |
| lighthouse-admin | `4e92542` | `/dashboard/onboarding` 화면 + 사이드바 '가입 설문' |

### ⚠️ 응답이 0건이면 집계가 '진짜 세는지' 알 수 없다

만들어 호출해보니 전부 0 이 나왔다. **맞게 짜서 0 인지, 틀리게 짜서 0 인지 구분이 안 된다.**
프로덕션 DB 에 가짜 데이터를 넣을 수는 없으니, **같은 모양의 파이프라인을 데이터가 있는 필드에
돌려서** 패턴 자체를 검증했다.

```
단일값 $group (gender)            → M:16, F:6          ← countBy 와 같은 모양
배열   $unwind (surveyResults)    → 정상 집계           ← byConcern 과 같은 모양
분모 비교  total 29 / onboarding 0 / gender 22
```

마지막 줄이 이 API 의 핵심이다 — **분모를 `total` 로 잡으면 비율이 통째로 어긋난다.**
화면에도 응답자/전체/답변없음 세 숫자를 나란히 놓아 함정을 드러냈다.

### 라이브 검증

```
GET /            → endpoints.admin.onboarding_stats 노출 (새 빌드 확인)
GET /api/admin/onboarding/stats (키 없이) → 401 "관리자 인증이 필요합니다"
admin.lighthouse.career/dashboard/onboarding → 307 → /login
```

> 💡 **Vercel 배포 반영을 판별하는 값싼 방법**: 보호된 라우트는 **307**(→/login),
> 없는 라우트는 **404** 다. 로그인 없이도 라우트 존재 여부를 알 수 있다.
> (`/dashboard/onboarding` 이 404 → 307 로 바뀌는 순간이 반영 시점)

**남은 것**: 화면 안쪽(실제 렌더링)은 로그인이 필요해 사람이 봐야 한다.
지금은 응답 0건이라 어차피 전부 빈 막대다 — v0.1.9 이후 가입부터 쌓인다.
