# 개발일지 2026-07-01

## 랜딩 파비콘 흰 배경 제거 (투명 배경 재생성)

### 배경
- 랜딩 파비콘이 "Nuxt 기본"으로 보인다는 제보 → 서버 검증 결과 실제로는 브랜드
  파비콘 정상 배포, **브라우저 파비콘 캐시** 문제로 확인 (라이브 md5=로컬 일치).
- 후속 확인: 파비콘에 **흰색 사각 배경이 불투명하게 구워져** 있었음 (모서리 alpha=255).
  기존 realfavicongenerator 입력 이미지가 흰 배경 포함본이었던 게 원인.

### 조치
- 원본 `assets/images/section1_symbol.png`(투명본, 478×568)에서 파비콘 전체 재생성.
- 도구: ImageMagick/PIL/sharp 부재 → macOS `sips`로 568×568 투명 정사각 패딩 후 크기별 리사이즈.
- `favicon.ico`는 16/32/48 멀티사이즈 PNG-in-ICO로 교체.
- public/ 아이콘 25개 교체(favicon/android/apple/ms), 전 파일 모서리 alpha=0 검증.
- 캐시 재발 방지로 `nuxt.config` 아이콘 href에 `?v=2` 부여.

### 트러블슈팅
- 1차 배포 실패: `?v=2`를 `rel="manifest"` href에도 붙여 Nuxt 프리렌더 크롤러가
  `/manifest.json?v=2`를 라우트로 오인 → 404 → 빌드 실패.
- 교훈: **Nuxt manifest href에는 쿼리스트링 금지.** manifest만 원복 후 재배포 성공.

### 결과
- www.lighthouse.career 파비콘 투명 배경 반영 완료·검증 (CloudFront 무효화 Miss 확인).
- 잔여(경미): iOS apple-touch-icon은 투명 시 홈화면에서 검정 배경 합성됨. 필요 시 apple만 배경색본 별도 생성.

---

## 검사 진행상황 브라우저 영속화 (lighthouse-test)

- 문제: 개발 중 새로고침/HMR 시 검사 진행상황이 계속 리셋. `useSurvey.ts` 상태가 메모리에만 존재 + `loadSurvey`가 currentPageIndex=0 강제 리셋.
- 조치: answers·currentPageIndex·scaleType 를 `lh_survey_progress_v1` localStorage 영속화(watch deep), 복원 시 loadSurvey는 리셋 대신 범위 클램프, 제출 완료 시 정리. 개발자 "진행상황 초기화" 버튼에도 연동.
- **핵심 발견**: 서버가 설문 조회 시 **문항 순서를 매 요청 랜덤화** → 답변은 ID로 저장돼 정확하나 새로고침 시 페이지 구성이 바뀌어 어긋남. 설문 구조(surveyData)를 `lh_survey_form_v1`에 캐시해 진행 중 동일 순서 재사용으로 해결.
- 검증: Playwright로 새로고침 후 위치·문항순서·답변 복원 확인. test.lighthouse.career 배포.
- 부수: SCSS `@import → @use` 마이그레이션(Dart Sass deprecation).

## 진로백과 준비과정·채용 탭 템플릿 (lighthouse-test)

- 대상: 예술·디자인·방송관리자(013601), 백엔드 API 미구현이라 jobCode별 샘플 하드코딩.
- 준비과정 탭: 현직자별 준비 여정 카드(사람 이모지, 프로젝트 나열, 루틴 별도 블록, 카테고리 요약, 개성 있는 한줄소개).
- 채용 탭: 상세 공고 카드(회사·D-day, 모집포지션, 고용형태, 주요업무/자격요건/우대사항).
- test.lighthouse.career 배포.

## 진로계획 탐색 — 예술 템플릿 추가 + '내 계획에 붙여넣기' (lighthouse-api + test)

- 탐색 템플릿은 API(`/api/career-plan/templates` → `PublicCareerPlan`)에서 로드. 기존 3개(마케팅)뿐이라 예술·디자인·방송관리자 템플릿 1개 추가(프로젝트 12개, 주차별 커리큘럼 포함).
  - seed-public-plans.js에 Plan4 추가 + PLANS export/require.main 가드, `scripts/apply-art-plan.js`로 해당 플랜만 비파괴 upsert(기존 3개 미변경). Atlas 3→4 반영.
- FE: 프로젝트 상세의 "내 계획에 붙여넣기" 실동작화(템플릿 프로젝트 복제→draftPlan 추가→planId 있으면 서버 동기화→구성 화면 이동, 중복 방지). 주차별 커리큘럼은 기존에 이미 동작.
- test.lighthouse.career + API(main) 배포.

## 다음 작업(예약)
- app.lighthouse.career 파비콘 작업 (랜딩과 동일 브랜드 파비콘, 투명 배경). `v*` 태그 배포 필요.
