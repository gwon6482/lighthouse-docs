# 개발일지 2026-08-28

## 타임라인 주차 전환 배포 — 서버 먼저, FE 나중

8/27에 구현만 해두고 미커밋으로 남아 있던 **타임라인 배치 월별 → 주차별 전환**을 배포했다.
FE와 서버 스키마가 함께 바뀌는 변경이라 순서가 중요했다.

| 대상 | 커밋 / 태그 | 결과 |
|---|---|---|
| `lighthouse-api` | `cce2853` | `Build & Deploy API` run 33147384945 success |
| `lighthouse-test` (스테이징) | `8438439` (main=dev) | `Deploy TEST` run 33147620244 success |
| `lighthouse-test` (프로덕션) | **`v0.1.8`** | `Deploy PROD` run 33148109016 success |

### 왜 서버가 먼저인가
구 서버의 `TimelineSlotSchema.month`는 `required`였다.
신 FE가 보내는 `{ week, projects }`를 구 서버가 받으면 `month` 없다고 거절한다.
반대로 구 FE가 보내는 `month` 슬롯은 **신 서버가 조용히 버릴 뿐** 깨지지 않는다.
비대칭이므로 서버를 먼저 올리는 쪽이 안전하다.

### ⚠️ Actions success ≠ 라이브 반영
`Build & Deploy API`가 하는 일은 이미지 푸시 + `create-container-service-deployment` **호출**까지다.
Lightsail nano가 이미지를 받아 헬스체크(interval 10s × healthyThreshold 2)를 통과하기까지
**Actions 성공 후 약 3분**이 더 걸렸다. 그 사이 라이브 swagger는 여전히 `month`를 서빙했다.

→ Actions 초록불만 보고 FE를 밀었다면 그 3분 동안 신 FE ↔ 구 서버 조합이 된다.
**swagger가 `week`를 서빙할 때까지 폴링한 뒤** FE를 밀었다(17회 폴링, 10초 간격).

라이브 확인용 swagger 경로는 `/api-docs/swagger.json`이다 (`/api-docs.json`, `/swagger.json`은 404).

### 서버 변경 (`cce2853`)
- `TimelineSlotSchema`: `month(required)` 폐기 → `week: Number`(1-based 시작주차). `month`는 legacy optional로 잔존
- 시작주만 저장하고 **점유 구간은 저장하지 않는다** — 저장하면 프로젝트 기간 수정 시 둘이 어긋난다
- `_weekFromLegacyMonth`: `week` 없는 구 문서를 `startDate` 기준으로 환산해 read 시 내려줌
- `saveTimeline`: week 정수·1이상·프로젝트 존재 검증 통과분만 저장
- `CurriculumWeekSchema.description` 추가(주차당 설명 1건). `items`는 레거시 읽기 호환으로 유지

커밋 전에 `models/CareerPlan.js`·`routes/careerPlan.js`의 주석이 1차 구현 시절 문구
("startDate 부터 7일 블록")로 남아 있는 걸 발견해 정정했다. 실제 규칙은 **월~일 달력 주**다.

### 검증
| 항목 | 결과 |
|---|---|
| `vue-tsc --build tsconfig.app.json` | 0건 |
| `build:test` / `build:app` | 둘 다 성공 |
| `generate-month-weeks.mjs` 재실행 | 산출물 diff 0 (멱등) |
| month-weeks 테이블 전수 대조 | 888개월 불일치 0건, 5주차 달 309개월 |
| 라이브 (test / app) | 신규 placeholder "이 주차에 무엇을 할지 적어보세요" 존재, 구 "항목" UI 문구 0건 |

### ⚠️ 어제의 단위테스트가 레포에 없었다
8/27 작업로그는 "주차규칙 29건 / 주간일정 11건 통과"를 기록했지만,
**해당 spec 파일이 레포에 존재하지 않는다.** 임시로 돌리고 남기지 않은 것으로 보인다.
배포 시점에 재실행이 불가해, 핵심 명제(월별 주차 수 = 그 달 월요일 개수)만
독립 계산으로 다시 대조했다.

→ **`usePlanTimeline` 회귀 테스트를 레포에 심는 게 후속 과제.**
통과 기록을 로그에 남기는 것과 테스트를 레포에 남기는 건 다르다.

### 번들 실측할 때 `grep -c`를 믿지 말 것
minify된 청크는 한 줄이라 `grep -c`(매칭 **줄** 수)가 항상 1이다.
"구 문구 1건 발견"처럼 보여도 실제로는 0건일 수 있다. `grep -o | wc -l`로 세야 한다.

### 운영 데이터 리스크 — 확인 후 생략
주 경계가 바뀌어 서버에 저장된 `WeeklySchedule.weekStart`가 새 계산과 어긋난다.
크래시는 아니고 **구 레코드가 조회되지 않고 그 주 일정이 새로 자동 생성**되는 형태다
(손으로 고쳐둔 주간일정이 자동생성본으로 되돌아가 보이고, 구 레코드는 고아로 남는다).

운영 Atlas를 직접 세보려 했으나 차단돼, 사용자에게 확인받아
**실사용 데이터가 없음**을 근거로 마이그레이션 없이 배포했다.
실사용자가 생긴 뒤 같은 성격의 주 경계 변경을 하려면 마이그레이션이 선행돼야 한다.

### 참고 — 기존 부채 (이번 변경과 무관)
루트 `pnpm type-check`는 2건 실패한다. 모노레포 전환 잔재:
`vitest.config.ts`가 없는 루트 `./vite.config`를 참조하고, `tsconfig.vitest.json`의 include 대상이 0개다.
소스 자체는 `tsconfig.app.json`으로 돌리면 0건이다.
