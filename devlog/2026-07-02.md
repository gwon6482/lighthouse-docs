# 개발일지 2026-07-02

## app/test 파비콘 브랜드 등대 교체 + theme 색상 수정 (lighthouse-test)

### 배경
- apps/app · apps/test 는 모노레포 루트의 **공유 publicDir**(`lighthouse-test/public`)를 사용.
- 기존 파비콘이 **Vue 기본 로고(초록 V)**, theme_color/theme-color 도 `#42b883`(Vue 초록)이었음.

### 조치
- 소스: 랜딩과 동일한 브랜드 등대 `lighthouse-landing/assets/images/section1_symbol.png`(투명).
- 도구: ImageMagick/sharp/PIL 부재 → `sips`(리사이즈) + Python(흰배경 알파합성/PNG 인코딩) 하이브리드.
- 생성/교체(public/ 5개):
  - 탭 파비콘(투명): `favicon-196.png`, `favicon.ico`(16/32/48 PNG-in-ICO, 기존 32단일 개선)
  - 흰배경 합성(홈화면 검정·마스커블 크롭 방지): `apple-icon-180.png`(로고82%),
    `manifest-icon-192/512.maskable.png`(로고72%=세이프존)
- `theme_color`/`theme-color` `#42b883` → `#ffffff` (app·test index.html + 양쪽 vite PWA manifest, 4곳).

### 배포/검증
- dev 커밋 → main 머지(push→deploy-test) + 태그 `v0.1.1` push(→deploy-app 프로덕션). 두 워크플로우 success.
- 검증: app.lighthouse.career · test.lighthouse.career 모두 favicon.ico md5=생성본 일치(브랜드),
  manifest theme_color=#ffffff, CloudFront Miss(최신본). 공유 public이라 test/app 동시 반영.
- 설계: 흰 배경 아이콘 + 흰색 theme 확정(추후 브랜드 옐로로 변경 가능).
