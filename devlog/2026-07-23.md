# 개발일지 2026-07-23

## R3 — career-design draft 자동저장/복원 (새로고침 유실 방지)

- **문제**: 진로계획 작성 중(플랜/프로젝트/루틴/타임라인) 새로고침·앱종료 시 전부 유실.
  useCareerDesign.ts의 draft 상태가 메모리 전용 reactive였음.
- **조치**: 설문 `useSurvey.persistProgress` 패턴 이식.
  - localStorage 키 `lh_cd_draft`에 { draftPlan, draftRoutine, draftProject, draftTimeline,
    editingProjectId, editingRoutineId } 스냅샷 저장.
  - 모듈 로드 시 `loadDraft()`로 복원(기본값에 스프레드 병합).
  - `watch([...draft], persistDraft, { deep: true })`로 변경 시마다 자동저장.
  - 신규 시작(startPlan→resetDraftPlan)/완료 시 reset이 draft를 비우면 watch가 빈 상태로
    갱신 → 이전 draft가 되살아나지 않음.
- **검증**: vue-tsc 0에러, build:app 성공. (UI 전체 플로우 구동 검증은 미실시 — 스모크 테스트 권장)
- frontend/progress.md "네비게이션/뒤로가기 안전 기준" 조치 대상 R1~R5 전부 완료 체크.

## 자기이해 검사 첫 페이지 뒤로가기 → 초기화 확인

- 검사 시작 이후 첫 페이지에서 뒤로가기 시 "지금까지의 검사를 초기화하고 답변방식을 바꾸시겠습니까?"
  확인 → 동의 시 로컬 결과 초기화 후 답변방식 선택(/self-understanding/select)으로.
- useSurvey.resetSurvey() 추가(answers 리셋 + currentPageIndex=0 + clearPersistedProgress).
- SelfUnderstandingTestPage onBeforeRouteLeave 가드: 제출 통과(bypass) / 진행 중(page>0) 뒤로는
  이전 문항으로 / 첫 페이지 뒤로는 초기화 확인. 온스크린 '이전' 버튼도 첫 페이지 활성화(handlePrev).
- 하드웨어 백·브라우저 백·온스크린 '이전' 모두 커버. surveyData(문항)는 유지(답변방식만 변경).
- vue-tsc 0에러, build:app 성공.

## 자기이해 검사 결과 페이지 분리 + 진로백과 팝업 (FE, 미커밋)

- 온보딩(최초검사) 결과 페이지를 별도 컴포넌트/라우트로 분리, 기존 returnTo 조건부 렌더링 제거.
  - 신규: SurveyResultBody(공통 본문, job-click emit), OnboardingSurveyResultPage,
    JobOverviewModal, JobOverviewCard(개요 전용 카드).
  - 온보딩 결과에선 추천 직업 클릭 시 진로백과 이동 대신 개요 팝업(진로백과 이동 차단으로
    '다음으로' 사라지던 버그 해소). 팝업 카드: 직업명 안으로, 개인·업무요소 제거, 안내문구,
    좋아요(북마크) 추가.
  - complete 페이지가 lh_survey_return 유무로 두 결과 라우트 분기.
  - '다음으로' 버튼 여백 조정(사이드 0→1.25rem 등).
  - ⚠️ lighthouse-test 로컬 미커밋 상태(배포 안 함).

## 설문 문항 카피 교정 (DB 반영 완료)

- survey_questions 문항 텍스트 22건 프로덕션 Atlas 직접 교정. 상세는 db-api/progress.md 참고.
  T1 오탈자5+따옴표2+주어통일10, T21 질문형1, T23 오탈자2, T3 파트명2. T3 어투통일은 보류.

## 남은 것
- 작성 폼 웹 새로고침 경고(beforeunload)는 R3로 유실 자체가 사라져 우선순위 낮아짐(선택).
- FE 결과페이지 분리/팝업 작업 로컬 미커밋 → 검토 후 커밋·배포(test=main / app=v* 태그).
- T3 존댓말 질문 어투 통일(해요체) 정책 미정 → 보류.
